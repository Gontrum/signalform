# Coverage reports
coverage/
.nyc_output/

# Build outputs
dist/
build/

# Dependencies
node_modules/

# Machine-generated runtime config — saveConfig rewrites it at runtime, so
# prettier reformatting only creates churn (git-ignored anyway).
config.json
